describe("My Use case", () => {
  beforeEach(() => {
    return;
  });

  describe("When a new Hugging Face repo is created, ", () => {
    it("adds a new Model in our local 'copy' (modelRepo) ", () => {
      return;
    });
  });
});
